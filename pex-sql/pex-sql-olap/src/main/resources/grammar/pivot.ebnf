grammar sql-pivot;

// PIVOT and UNPIVOT operations

pivot-clause ::= 'PIVOT' '(' aggregate-func '(' column-name ')'
                 'FOR' column-name 'IN' '(' value-list ')' ')' ;

unpivot-clause ::= 'UNPIVOT' '(' value-column
                   'FOR' name-column
                   'IN' '(' column-list ')' ')' ;

aggregate-func ::= 'SUM' | 'AVG' | 'COUNT' | 'MIN' | 'MAX' ;

value-list ::= literal-value (',' literal-value)* ;
column-list ::= column-name (',' column-name)* ;

value-column ::= identifier ;
name-column ::= identifier ;
column-name ::= identifier ;
identifier ::= /[a-zA-Z_][a-zA-Z0-9_]*/ ;
literal-value ::= number | string-literal | 'NULL' ;
number ::= /[0-9]+/ ;
string-literal ::= /'[^']*'/ ;
