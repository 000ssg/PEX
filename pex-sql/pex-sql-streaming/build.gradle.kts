description = "PEX SQL Streaming — Streaming SQL, windowed aggregation, event processing"

dependencies {
    implementation(project(":pex-sql:pex-sql-core"))
    implementation(project(":pex-base"))
}
